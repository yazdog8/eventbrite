import React, { useEffect } from "react";
import PropTypes from "prop-types";

const CartDisplay = (props) => {
  useEffect(() => {}, []);

  return <div>Cart Display</div>;
};

CartDisplay.propTypes = {};
CartDisplay.defaultProps = {};

export default CartDisplay;
